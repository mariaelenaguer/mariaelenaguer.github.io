# frozen_string_literal: true

source "https://rubygems.org"

# Matches the gem set GitHub Pages builds this site with, so a local
# build reproduces production (including the plugins Pages enables by
# default: jekyll-relative-links, jekyll-optional-front-matter, etc).
gem "github-pages", group: :jekyll_plugins
